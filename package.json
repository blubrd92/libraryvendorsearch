{
  "name": "library-vendor-search",
  "version": "7.9.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the Library Vendor Search browser extension (Chrome + Edge + Firefox).",
  "scripts": {
    "package:extension": "node build-zips.mjs"
  },
  "devDependencies": {
    "webextension-polyfill": "0.12.0"
  }
}
